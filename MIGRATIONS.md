# Migrations

There is no migration tool. The base schema was built by hand in the Supabase dashboard, and every
change since is a SQL file at this directory's root, pasted into the **Supabase SQL editor** by a human.
DDL cannot be run from the app — the service-role key reaches PostgREST, which does not do DDL.

**This file is the only record of what has actually been applied. Update it when you run something.**

The failure mode it exists to prevent: a SQL file gets written, the code that depends on it gets written
too, typecheck and build both pass — and the app only breaks at runtime against the real database. That
has now happened twice.

## Rules

- A new SQL file must be idempotent (`create ... if not exists`, `add column if not exists`, `do $$ ... end $$`
  guards) so it is safe to re-run.
- New tables must follow `ENABLE_RLS.sql`: `enable row level security` with **no policies**, plus
  `revoke all ... from anon, authenticated`. The backend uses the service-role key, which bypasses RLS.
- Ship the SQL file and the code that needs it together, then run the SQL **before** relying on it.
- The Supabase SQL editor runs a script as a single transaction — a failure part-way rolls back the
  *whole* file. Do not assume a script that errored applied "the earlier bits".
- **Base-schema primary keys are not all `uuid`.** They were made by hand, so `properties.id` is
  `text` and `billing_ledgers.id` is `character varying`, even though the app fills every one of
  them with a `crypto.randomUUID()` string. A new child table that declares its FK column `uuid`
  fails with `42804: Key columns ... are of incompatible types`. Declare referencing columns `text`
  unless you have checked the parent, and `::text` any id you select in a backfill. Tables created
  by these migration files (`staff`, `accounts`, …) *are* real `uuid`.
- Prefer a self-contained script over an ordering dependency: re-stating another migration's
  `add column if not exists` costs nothing and removes a whole class of "ran them in the wrong
  order" failures. Guard anything touching an optional module's table with
  `if to_regclass('public.x') is not null`.
- **Don't trust this table — probe the database.** Because it is updated by hand *after* a
  manual SQL-editor run, it drifts whenever someone runs a file and forgets. On 2026-08-06 every
  one of the ten files listed here as pending turned out to be applied. Confirming takes seconds:
  `select` one column the migration adds and see whether PostgREST answers or returns
  `PGRST205` / `PGRST204`. A ledger that is wrong in the *safe* direction still teaches everyone
  to ignore it, which is how the failure it exists to prevent comes back.

## Applied

| File | Status | What it does / what breaks without it |
|---|---|---|
| `ENABLE_RLS.sql` | ✅ applied 2026-07-12 | RLS on every table, zero policies, grants revoked from `anon`/`authenticated`. Without it the anon key reads all tables. Verified: anon key now 401s. |
| `ADD_WEBPUSH.sql` | ✅ applied | Adds `p256dh` / `auth` / `role` to `device_tokens`, unique on `token`. Without it web-push subscription registration fails. |
| `FIX_PUSH_AND_TENANT_MOVE.sql` | ✅ applied 2026-07-14 | Adds `tenants.owner_id` (+ backfill), makes `tenants.property_id` nullable, retypes `device_tokens.user_id` uuid→text and drops its FK + RLS policies. Without it `GET /api/admin/tenants` 500s (owner login is dead) and tenant push registration fails. **Originally aborted** on `0A000: cannot alter type of a column used in a policy definition` — fixed by adding step 0, which drops the dependent `device_tokens` policies first. |
| `ADD_SUPPORT_TICKETS.sql` | ✅ applied 2026-07-14 | Creates `support_tickets` (owner → system-admin tickets, `submitted → assigned → in_progress → done`, `assigned_to` / `finished_at`). Without it the owner Support tab and admin Tickets queue 404. |
| `ADD_TENANT_LOGIN_CONTROL.sql` | ✅ applied 2026-07-15 | Adds `tenants.allow_login_unassigned` (default `false`). Lets owners block an unassigned tenant from signing in, with a per-tenant override. Without it, tenant login and `GET /api/admin/tenants/me` 500 on the missing column. |
| `ADD_PASSWORD_RESET_HISTORY.sql` | ✅ applied (confirmed by probe 2026-08-06) | Creates `password_reset_history` (append-only audit of owner password changes: `admin_reset` / `self_service_email` / `self_change`). Admin-only read via `/api/super-admin/password-resets`. Without it, the reset routes' audit writes fail (non-fatal) and the admin Reset-log tab 500s/empties. |
| `ADD_CONTACT_MESSAGES.sql` | ✅ applied | Creates `contact_messages` (owner "Contact us" enquiries from the custom plan card, `new → in_progress → resolved/archived`). Without it, the owner contact form POST and the admin Messages queue 404/500. ⚠️ Applied with `tier_id uuid`, which rejects the text tier slugs (`whole_building`) — see `ALTER_CONTACT_MESSAGES_TIER_ID_TEXT.sql`. The `.sql` file has since been corrected to `text` for fresh installs. |
| `ALTER_CONTACT_MESSAGES_TIER_ID_TEXT.sql` | ✅ applied (confirmed by probe 2026-08-06 — `tier_id` accepts text and holds `whole_building`) | Retypes `contact_messages.tier_id` uuid→text so tier slugs (`whole_building`, `free_tier`, …) can be stored. Without it the "Whole Building" Contact-us POST 500s with `invalid input syntax for type uuid: "whole_building"`. |
| `ADD_PAYMENT_SUBMISSIONS.sql` | ✅ applied (confirmed by probe 2026-08-06) | Creates `payment_submissions` (owner manual bKash payments awaiting admin approval, `pending → approved/rejected`). On approval a `subscription_history` row activates the plan; a pending row writes none. Without it the owner payment POST and admin Payments queue 404/500. |
| `ADD_APP_SETTINGS.sql` | ✅ applied (confirmed by probe 2026-08-06) | Creates `app_settings` (key/value singleton for `payment_config` + `default_signup_tier`). Without it the Payment Setup menu, the owner payment screen's QR, and the default-signup-tier setting have nowhere to read/write. (QR image reuses the existing public `RentMasterProDocs` bucket via `/api/admin/uploads` — no new bucket.) |
| `ADD_REMINDERS.sql` | ✅ applied (confirmed by probe 2026-08-06) | Creates `reminders` (owner-scheduled rent reminders to tenants, `pending → sent/canceled`, once/monthly). Without it the owner Reminders tab POST/GET and the `/api/cron/reminders` tick 404/500. Also needs `CRON_SECRET` env + `vercel.json` cron for scheduled (future/monthly) delivery. |
| `ADD_BILLING_PAID_AT.sql` | ✅ applied (confirmed by probe 2026-08-06 — `billing_ledgers.paid_at` present; also re-added by `ADD_BILLING_PAYMENTS.sql`) | Adds `billing_ledgers.paid_at timestamptz` — the date the owner says the rent was actually received (they pick it when marking an invoice paid, and can correct it afterwards). Referenced by the code since receipts landed, but never had a migration file. Without it the PATCH logs `paid_at not updated`, every receipt prints today's date, and a back-dated invoice is stamped LATE even when the tenant paid on time. |
| `ADD_BILLING_PAYMENTS.sql` | ✅ applied (confirmed by probe 2026-08-06 — `billing_payments` and `billing_ledgers.amount_paid` both present) | Creates `billing_payments` (installment log per invoice) + `billing_ledgers.amount_paid`, allows `payment_status = 'partial'`, backfills a payment row for every already-paid invoice, and re-points `account_transactions.source_ref` from the ledger id to the payment id. Without it every partial-payment route 404/500s, the owner's Collected/Outstanding figures are wrong, and marking a bill paid fails on the missing column. ⚠️ Section 3 drops any hand-made CHECK on `payment_status` — the table has no DDL in this repo, so an unknown constraint there would otherwise reject `'partial'` at runtime only. |
| `ADD_PLAN_TENURE.sql` | ✅ applied 2026-08-06 on the **second** attempt (the first failed and rolled back — see below) | Adds `subscription_tiers.duration_days` (custom tenure, e.g. a 7-day plan) plus idempotent `is_active` / `discount_percent`, allows `billing_interval = 'days'`, **drops the NOT NULL on `subscription_history.expiry_date`**, and repairs the year-2126 rows. ⚠️ The first attempt aborted with `23502: null value in column "expiry_date"` because that column was NOT NULL and the repair writes null for perpetual plans — and since the SQL editor runs a file as one transaction, **nothing at all was applied**. §4 now drops that constraint before the repair. The constraint also had to go regardless of the repair: `computeExpiry()` returns `Date | null` and every insert site writes null for a tier with no billing period and no price, i.e. the `whole_building` "Contact us" tier, so admin-assigning that plan would 500 with the same error. **`is_active` and `discount_percent` had no migration file at all** despite `/api/super-admin/tiers` writing them since discounts shipped. Without this file, plan create/edit still works (both routes retry without `duration_days` on 42703 and log loudly) but custom-length plans silently fall back to their billing interval. ⚠️ `'days'` is deliberately a different `billing_interval` value from `'custom'` — `'custom'` is the "Contact us" / Whole Building convention and `lib/subscription.ts` excludes it from the free-baseline lookup. |
| `ADD_NOTICE_TARGETS.sql` | ✅ applied (confirmed by probe 2026-08-06 — `notices.target_owner_id` present) | Adds `notices.target_owner_id` + indexes and widens the `target_scope` CHECK to the five audiences (`everyone`, `all_owners`, `all_tenants`, `individual_owner`, `individual_tenant`). Without it, admin circulations to a single owner fail and `/api/admin/notices` GET falls back to a reduced clause list. **Was never listed in this table at all** despite shipping on 2026-07-24. |
| `ADD_STAFF.sql` | ✅ applied (confirmed by probe 2026-08-06 — `staff` + `owner_addons` present) | Creates `staff`, `staff_payments`, `owner_addons` (the per-owner paid-module grant) and `subscription_tiers.staff_included`. Without it the owner Staff tab 404/500s and every add-on grant fails. **Was never listed in this table.** |
| `ADD_ACCOUNTS.sql` | ✅ applied (confirmed by probe 2026-08-06 — `accounts` + `account_transfers` present) | Creates `accounts`, `account_transactions`, `account_transfers` and `subscription_tiers.accounts_included`. Without it the owner Accounts tab and the invoice-paid / salary automations 404/500. **Was never listed in this table.** |
| `FIX_DEVICE_TOKEN_TRANSPORT.sql` | ✅ consistent with having run (2026-08-06: 0 web-push rows mislabelled `android`) | Repairs `device_tokens` rows whose `device_type` misdescribed their transport, which routed Web Push subscriptions to FCM and silently killed delivery. ⚠️ **Pure data repair — it changes no schema, so unlike every other row here its applied-state cannot be proven by probing columns.** The check is behavioural: no row should carry `p256dh`/`auth` *and* `device_type = 'android'`. **Was never listed in this table.** |
| `ADD_PLAN_RECURRING.sql` | ✅ applied 2026-08-07 | Adds `subscription_tiers.is_recurring boolean not null default true` — one-time plans. `is_recurring = false` means the owner may take the plan once (a trial) and must then choose another; "already used" is simply "has a `subscription_history` row for it", so no extra table. Enforced server-side on **both** owner write paths (`/api/admin/subscription` POST and `/api/admin/payments` POST, code `ONE_TIME_PLAN_USED`), not just by disabling the button. The admin can still re-assign it. ⚠️ **Pairs with a change in `lib/subscription.ts`:** an expired one-time plan now resolves to the **Free** state instead of grace-then-lock. Without that, a trial — priced 0 but carrying a `duration_days`, which `tierIsFree()` excludes — would run the full paid lifecycle and lock every trial user who did not convert, ten days after their trial ended. Without this file every plan stays renewable exactly as before (the code tests `is_recurring === false`). |
| `ADD_PLAN_VISIBILITY.sql` | ✅ applied 2026-08-07 | Adds `subscription_tiers.is_public boolean not null default true` — hidden plans. `is_public = false` means not listed to owners and not self-selectable, but still admin-assignable, and still visible to an owner already on it so they can renew instead of silently lapsing at expiry. Deliberately **not** `is_active`, which retires a plan for everyone including the admin's own assign dropdown. Without this file, plan create/edit still works (the routes drop the column via `missingColumnFrom()` and log) and every plan simply stays listed — the code tests `is_public !== false`, so an absent column behaves exactly as before. Enforced server-side in four places via `tierVisibleToOwner()` in `lib/subscription.ts`. |
| `ADD_PRESENCE.sql` | ✅ applied 2026-08-06 | Creates `user_presence` (heartbeat log, PK `(user_id, device_id)`, `user_id` is `text` because it holds either an auth uid or a `tenants.id`) and `welcome_dispatch` (welcome-broadcast dedupe). Without them the admin console shows nobody as ever online, Analytics reports 0 online, and a repeat welcome send would message people twice. Verified live after the run: heartbeat → online, a 6-minute-old row reads offline, tenant heartbeats record `role='tenant'`, and a second welcome send is refused with 409. |
| `ADD_TENANT_NID_AND_RECEIPT_NAME.sql` | ✅ applied, **including the re-run** (confirmed by probe 2026-08-06: `nid_encrypted` + `receipt_name` present, and 2 tenant rows hold a NULL `nid_hash`, so the NOT NULL really was dropped) | Adds `tenants.nid_encrypted` (the NID is now AES-256-GCM encrypted instead of one-way hashed, so an owner can read it back and correct it — `nid_hash` becomes legacy and unrecoverable) and `properties.receipt_name` (per-property name printed on receipts; null = the owner's account name). Without it, onboarding or editing a tenant with an NID 500s on the missing column, and saving a property's receipt name fails. **Also needs the `NID_ENCRYPTION_KEY` env var** (32 bytes, base64 or hex) on the server, or saving an NID is refused with a message naming it. ⚠️ The added `alter column nid_hash drop not null` fixes onboarding failing with `null value in column "nid_hash" … violates not-null constraint`: the app stopped writing that legacy column, exposing a NOT NULL that had never been tested. |
| `ADD_APP_LOGS.sql` | ⏳ **PENDING — run this** | Creates `app_logs` (the diagnostic trail behind every error the user sees: `api` / `client` / `cron` / `email` / `push`). Read only by the admin Logs tab via `/api/super-admin/logs`; written by `lib/logger.ts` from `lib/api-response.ts`, the public `POST /api/logs` client-crash intake, and the push/email senders. `user_id` is **`text`** because it holds either an auth uid or a `tenants.id`, exactly like `device_tokens.user_id`, and it is deliberately not a FK — a log row must never be un-writable. Without it, every log insert fails (non-fatally — `lib/logger.ts` falls back to `console.error`, i.e. the state the app was in before), and the admin Logs tab shows nothing. ⚠️ Pairs with a behaviour change: 500 responses no longer echo the raw provider `err.message` to the browser. The caller gets a generic sentence plus a `requestId`, and the detail lives in this table under that id — so **without this table a 500 becomes genuinely undiagnosable**, which is the one reason to run it promptly. |
| `ADD_NOTIFICATION_PREFS.sql` | ⏳ **PENDING — run this** | Creates `notification_prefs` (`user_id` text PK, `sound` one of `custom`/`default`/`off`) — which sound a person's push notifications make. Keyed the same way as `device_tokens.user_id` (owner auth uid **or** tenant id) so `lib/push-send.ts` can look the preference up for every recipient of a dispatch in one query; deliberately not `user_metadata`, which tenants do not have. Read by `deliver()` in `lib/push-send.ts`, written by `/api/notifications/preferences`. Without it **nothing breaks**: the prefs lookup is wrapped and falls back to `custom` for everyone, which is the shipped default, but the Settings control silently fails to save. |
| `ADD_FIELD_ENCRYPTION.sql` | ✅ applied 2026-08-23 | Adds `staff.nid_encrypted`, `payment_submissions.sender_msisdn_encrypted` and `.txn_id_encrypted`, and **drops the legacy `tenants.nid_hash`**. Brings three fields up to the treatment `tenants.nid_encrypted` has had since `ADD_TENANT_NID_AND_RECEIPT_NAME.sql`: a staff national ID was stored in plaintext beside an ID scan and a photo, and a payment's sender number + transaction id together tie a person to a real financial transaction. All three are **encrypted, not hashed**, because they must be read back — the owner confirms a staff NID, an admin reconciles a payment by eye against the bKash statement. Safe to encrypt because none of them is ever queried: `staff` is indexed on `owner_id`/`property_id`, `payment_submissions` on `owner_id`/`status`/`created_at`, and there is no index, unique constraint or search over any of the three. Written via `staffFieldsFrom()` (`lib/staff.ts`) and `encryptSubmissionFields()` (`lib/payments/submissions.ts`); read back through `shapeStaffForOwner()` and `shapeSubmission()`, which every route returning those rows now goes through so the responses cannot drift. Without it, saving a staff NID or submitting a payment **fails on the missing column**. ⚠️ **Run this BEFORE deploying the code** — the new write paths target columns this file creates. ⚠️ The old plaintext columns are deliberately kept and the read paths fall back to them, so a half-finished migration is never destructive; run `node scripts/backfill-encryption.mjs` (supports `--dry-run`, safe to re-run) to convert existing rows, then drop them as a separate deliberate step. ⚠️ Also needs `FIELD_ENCRYPTION_KEY` on the server — the existing `NID_ENCRYPTION_KEY` is still read as a fallback, so an existing deployment needs no env change. Re-save admin → Payment setup once to encrypt `app_settings.payment_config.walletNumber`. |
| `ADD_TERMS_ACCEPTANCES.sql` | ✅ applied 2026-08-23 | Creates `terms_acceptances` — the append-only record of who accepted which version of the Terms and Privacy Policy, and when, written by `/api/auth/signup`. Deliberately a table and **not** `user_metadata`: GoTrue's `PUT /auth/v1/user` lets any signed-in owner write arbitrary metadata keys straight against Supabase with the anon key, so consent stored there would be forgeable by the very person it is evidence about — the same reasoning `lib/features.ts` gives for keeping add-on grants out of metadata. `owner_id` is `text` and not a FK, like `password_reset_history` and `plan_events`. Captures `ip` and `user_agent` alongside the version, which is what makes it evidence rather than a boolean. One row per acceptance rather than a column on `user_profiles`, so a future re-acceptance of an updated version is a genuinely new row (same shape as `plan_events`' `(owner_id, event, ref)` dedupe). Without it, **signup fails** — consent recording is not best-effort. |
| `ADD_BUILDINGS.sql` | ✅ applied 2026-08-23 | Whole Building plan, phase 1. Creates `buildings` (one per building admin) and `building_owners` (the flat-owner roster, PK on `owner_id` so an owner belongs to at most one building), and re-states the `subscription_tiers.staff_included` / `accounts_included` columns plus the `where billing_interval = 'custom'` update so the file is self-contained. Neither uid column is a FK to `user_profiles`, deliberately: that FK is NO ACTION and would fire during `auth.admin.deleteUser` **after** the delete cascade had already run, leaving a half-deleted account — the exact failure `properties.owner_id` once produced. Without it the building admin console 404s (`NO_BUILDING`) and `buildingMembershipOf()` returns null for everyone, which is the designed fail-safe: every existing owner resolves their plan exactly as they do today, so shipping the code ahead of the SQL is safe. |
| `ADD_BUILDING_BILLING.sql` | ✅ applied 2026-08-23 | Whole Building plan, phase 2. Creates `building_service_invoices` (the monthly service charge a building admin bills each flat owner) and `building_service_payments`. Same truth model as the rent cycle: the PAYMENT rows are the truth and the invoice's `amount_paid` / `payment_status` / `paid_at` are derived by `recalcBuildingInvoice()`. A unique index on `(owner_id, billing_month)` is what makes “generate for this month” safe to press twice. Deliberately has **no `'sent'` status** (no tenant-style “I have paid” claim exists here) and **no new `account_transactions.source` value** — paying an invoice books income with the existing `source = 'billing'` and `source_ref` = the payment id, so the CHECK constraint on that live money table is never touched. Requires `ADD_BUILDINGS.sql`. Without it the building Service-charge tab and the owner's statement 404/500; nothing else is affected. |
| `ADD_BUILDING_EXTRAS.sql` | ✅ applied 2026-08-23 | Whole Building plan, phase 3. Creates `building_amenities` (what the building runs and what each costs per month) and `building_income_sources` (rooftop, parking, signboard — the non-rent money it collects). Both are deliberately **thin config lists, not ledgers**: they hold definitions, nothing derives from them, and every actual transaction still lives in `account_transactions`. They exist so those categories stay consistent instead of being retyped as differently-spelled free text each month, and so a service-charge statement can print an itemised breakdown. Requires `ADD_BUILDINGS.sql`. Without it the building console's Setup tab 404s; nothing else is affected. |
| `ADD_BUILDING_NOTICES.sql` | ✅ applied 2026-08-23 | Whole Building plan, phase 4. Creates `building_notices` — the canonical PRINTABLE record of a building notice, carrying the reference number, issue date and delivered count a physical notice pinned to a lift door needs. The in-app copies are written by the POST route into the EXISTING `notices` table (one batched insert of `individual_owner` rows, or a single `all_tenants` row), so **there is no DDL on `notices` and `ADMIN_ONLY_SCOPES` is not relaxed** — relaxing it would let any owner address any other owner by uid. Requires `ADD_BUILDINGS.sql`. Without it the building Notices tab 404s; printing reports still works, since those read the real ledgers. |
| `ADD_BUILDING_LOGIN_IDS.sql` | ✅ applied 2026-08-23 | Adds `buildings.house_no` and `building_owners.flat_no` — the two inputs a system-generated login identifier is built from. Building-tier accounts stop having email addresses: a building admin signs in as `12a-678@bari360.com` (house + last 3 digits of mobile) and a flat owner as `12a-3b-456@bari360.com` (house + flat + last 3). It is **still just the email string** — `auth.users.email` is already unique and no route rewrites it, so there is no `login_id` column and no second source of truth; these two columns store the inputs, never the identifier. ⚠️ **`house_no` is nullable on purpose, and that is the entire migration story**: a building with no house number keeps the existing typed-email path forever, which is what lets old and new accounts coexist with no backfill. So this file is safe to run before or after the code ships — until an admin fills the number in, nothing changes. Neither column is unique, deliberately: house number "12" is not unique across a city, and a unique `(building_id, flat_no)` would refuse the second co-owner of one flat, which the `-2` suffix rule exists to serve. Without it, creating a building admin or a roster owner 500s on the missing column once an admin has entered a house number. Requires `ADD_BUILDINGS.sql`. |
| `ADD_PLAN_EVENTS.sql` | ⏳ **PENDING — run this** | Creates `plan_events` — the record of what an owner has already been told about their subscription (`expiring_soon` / `grace_started` / `downgraded`), written by `/api/cron/subscriptions` and read by the owner dashboard's one-time downgrade modal. The unique index on `(owner_id, event, ref)` is the whole point: `ref` holds the expiry date the event belongs to, so re-running the cron sends nothing, but a **renewed** plan gets a genuinely new event instead of being swallowed. Same dedupe shape as `welcome_dispatch` in `ADD_PRESENCE.sql`. `owner_id` is `text` and not a FK, like `password_reset_history`. Without it the cron 500s on every owner, `/api/admin/subscription` quietly returns `pendingEvent: null` (it catches and continues, so the Plan tab still works), and no expiry warning or downgrade notice is ever sent. ⚠️ Pairs with a behaviour change in `lib/subscription.ts`: once grace ends a paid plan now **drops the owner to Free** (2/2, add-ons off, excess items view-only via the existing `ITEM_DISABLED` machinery) instead of locking on the old tier's limits forever. `permissions_revoked` still hard-locks — an admin revoke is a deliberate act, not a billing event, and must not be softened into a downgrade. ⚠️ Also needs the `/api/cron/subscriptions` entry in `vercel.json` (added) and `CRON_SECRET` set, which it already is. |
| `ADD_BUILDING_PLANS.sql` | ⏳ **PENDING — run this** | Whole Building plan, phase 5: the commercial lifecycle. Creates `building_subscriptions` (one contract per building: tenure, term dates, pay-by deadline, per-building grace/warn windows), `building_plan_invoices` + `building_plan_invoice_items` (the itemised quote — **the line items ARE the pricing model**, which is why this feature adds no `subscription_tiers` row), `building_plan_payments` (the truth; the invoice's `amount_paid`/`payment_status`/`paid_at` are derived by `recalcPlanInvoice()` and written by nothing else), `building_plan_requests` (renewal requests **and** payment claims, one queue behind the admin console's new Buildings menu) and `building_plan_events` (the cron dedupe key). ⚠️ **Pairs with a behaviour change in `lib/subscription.ts`:** the `whole_building` tier is priced 0 with `billing_interval = 'custom'`, so it ran the FREE path and a building admin's plan was **perpetual — it could never expire, warn or lock**. `resolveOwnSubscription()` now OVERLAYS the contract state from `lib/building-plan.ts` onto the resolved plan (lifecycle fields only; `tierId`, `tierName` and `limits` are untouched, so the bundled modules and unlimited limits survive). Because `PLAN_GOVERNED_ROLES` already contains `building_admin`, that one overlay locks them at ~50 route call sites — and because `resolveOwnerSubscription()` substitutes a building admin's state into every flat owner beneath them, **an unpaid building takes its flat owners read-only with it**. That is the agreed scope: the building is the paying customer. ⚠️ Unlike the owner lifecycle, the terminal state is **LOCKED, not a drop to Free** — a whole building cannot operate on the 2/2 free baseline, and the fallback would strip the bundled modules from every flat owner. This revives `lockReason: 'expired'`, which time could no longer reach. ⚠️ **The file ends with a BACKFILL** giving every existing building a paid, one-year contract from the day it runs; without that they would all resolve as "never paid" and lock 15 days later. Correct their real dates from the Buildings menu. Deliberately does **not** extend `plan_events`' CHECK constraint (that migration is still unrun — a live ALTER depending on an unrun file is how one deploy breaks two features). Without this file **nothing breaks**: `buildingPlanOf()` returns null on a missing table, so every building resolves exactly as it does today (perpetual), the building console's new Plan tab says "no billing contract on file", and the admin Buildings menu lists buildings with no contract. |
| `ADD_DELETION_AUDIT.sql` | ✅ applied 2026-08-27 | Drops the `building_plan_invoices.building_id → buildings(id) on delete cascade` foreign key and adds the `building_name` / `admin_email` identity snapshot. Deleting a building admin deletes their `buildings` row, and that one cascade silently destroyed `building_plan_invoices` → `building_plan_invoice_items` → `building_plan_payments`, i.e. **the entire record of what we billed a building and what they paid us**. The column stays and keeps its value (a plain uuid pointing at a building that no longer exists, exactly like `building_plan_payments.building_id` and every other uid column here) rather than becoming `on delete set null`, so an archived building's invoices and payments stay grouped and `/api/super-admin/buildings/archived` can reconstruct it. The two child tables need no change — they cascade off the invoice, which now survives. Everything else hanging off `buildings` keeps its cascade on purpose; none of it is money paid to us. ⚠️ **Order matters, unlike the other building migrations.** They were all safe to ship code-first because `lib/building.ts` treats a missing table as "no building"; this one *is* the change, so deploying first leaves the next building deletion destroying the same data. ⚠️ Requires `ADD_BUILDING_PLANS.sql`; the whole file is wrapped in a `to_regclass` guard that raises a `SKIPPED` notice instead of aborting, because the SQL editor runs a file as one transaction and a bare `42P01` would apply nothing at all (how `ADD_PLAN_TENURE.sql` failed its first attempt). Pairs with `lib/account-purge.ts`, which stops purging `payment_submissions`, `subscription_history`, `support_tickets`, `contact_messages` and `password_reset_history`, and starts purging the seven tables the old cascade orphaned. ⚠️ **In practice the order was inverted:** the code deployed on 2026-08-26 and this ran on 2026-08-27, so for roughly one day the delete route offered to remove a building admin while the cascade was still live. If any building account was deleted in that window its plan invoices, line items and payments are gone and are not recoverable from the app. |
| `ADD_PUBLIC_CONTACT.sql` | ✅ applied 2026-08-27 | One line: `alter table public.contact_messages alter column owner_id drop not null`. `owner_id` was `uuid not null` because the only "Contact us" button lived on the Whole Building plan card *inside* an owner's account. The new public `/plans` page shows that same tier to people who have not signed up, and since it is priced offline, **"contact us" is its only call to action** — without a nullable owner_id that card is a dead end for exactly the audience it exists to attract. `POST /api/app/contact` writes `owner_id: null`; until this runs, every public enquiry fails with `23502` and the visitor sees a generic error on a form that looks like it should work (the route logs a named warning when it sees that code). NULL means "not one of our owners", which is worth being able to see: the admin Messages queue already falls back to the row's own `name`/`email`/`phone`, which the public form requires, so a null owner renders correctly with no change to that screen. The column stays **not** a foreign key, as before. Wrapped in the same `to_regclass` guard as `ADD_DELETION_AUDIT.sql`. ⚠️ This is the second route in the app where an anonymous caller can write a row (the other is `/api/logs`) — it is throttled to 5 per IP per 15 minutes, caps every field, and refuses an enquiry with neither an email nor a phone number. |
| `UPDATE_WHOLE_BUILDING_PLAN.sql` | ✅ applied 2026-08-27 | Not schema: a single idempotent `UPDATE` on the `subscription_tiers` row `id = 'whole_building'`, rewriting its `name` and `description`. Those two fields are rendered raw as `{tier.name}` / `{tier.description}` by the public `/plans` page and the owner Plan tab, so the paragraph above the feature bullets is DATA and cannot be changed in code — the bullets beside it are in the React source and were updated in the same change. The old wording leaned on an unqualified "maintenance", which to a building committee reads as maintenance of the BUILDING — the one thing the contract does not cover; the new text says software maintenance and support, and names the rest of what the contract includes (custom feature work, app updates, content-change help, own domain on request). Without it **nothing breaks** — the card simply keeps quoting the old paragraph beneath the new bullets, which is merely inconsistent, not wrong. Safe to re-run. The identical edit can be made without SQL in Admin → Plans → edit "Whole Building"; the file exists so the wording is reviewable and version-controlled alongside the card copy it sits on. |
| `UPDATE_PLAN_COPY.sql` | ⏳ **PENDING — run this** | Not schema: two idempotent `UPDATE`s giving `free_tier` and `premium_monthly` real `name` and `description` text. Both rows still carried placeholder copy that had never been rewritten and was live on the public pricing page — "Free Baseline Tracking / Provides basic mapping operations up to 1 distinct physical property footprints" and "Premium Pro Plan Upgrade / Provides enterprise scaling capabilities, custom background parameters, and automated browser alerts layers". Neither describes anything this app does. Found while translating the plan cards: `{tier.name}` / `{tier.description}` now pass through `t()`, and translating that text would only have produced nonsense in two languages. ⚠️ **PAIRS WITH `lib/locales/bn.ts`** — the English string IS the dictionary key, so the Bangla entries added in the same change mirror these strings byte for byte, em dashes included. Run this and deploy together: if the copy changes and the keys do not, the cards silently fall back to English for Bangla readers, which is the exact failure the batch was fixing. Deliberately does **not** touch `free_tier`'s 1 property / 1 tenant limits — those are correct; only the words were wrong. Safe to re-run. Same edit is possible in Admin → Plans. |
| `ADD_BUILDING_OWNER_FLATS.sql` | ✅ applied 2026-08-28 | Whole Building, phase 6: one owner, several flats, one login. Creates `building_owner_flats` (one row per flat: label, token, its own `default_service_charge`, `is_primary`, and `property_id text` → the rentable unit auto-created behind it) and adds `flat_id` + `flat_label` to `building_service_invoices`. ⚠️ **`property_id` is `text`, not `uuid`** — `properties.id` is text (see the base-schema note at the top of this file); `uuid` fails with `42804`. ⚠️ **`building_owners` is deliberately NOT widened.** Its `primary key (owner_id)` is the index `buildingMembershipOf()` uses on every plan resolution, and that function ends in `.maybeSingle()` whose error branch resolves the owner to null — no building, therefore the FREE plan — and caches it for 60s. A second roster row would make "this owner bought a second flat" a silent plan downgrade, so the flats live in a child table and `lib/building.ts`'s existing functions are untouched. `building_owners.default_service_charge` is **frozen**, not dropped (a drop is irreversible in a one-transaction editor, and leaving it keeps a code rollback viable); `unit_label`/`flat_no` become login provenance, mirroring the primary flat. ⚠️ **RUN BEFORE THE DEPLOY** — unlike `ADD_BUILDINGS.sql`, this one is not tolerable code-first: the new code SELECTs the table and INSERTs the columns, so deploying first takes owner creation (`PGRST205`) and invoice generation down. Running it early is completely inert: production reads none of it, and the **guard split** keeps old rows guarded throughout — `(flat_id, billing_month)` for new rows, a partial `(owner_id, billing_month) where flat_id is null` for old ones, both created before the original index is dropped, all in one transaction. ⚠️ **The backfill has TWO passes and the second is the one that matters**: every roster row becomes a primary flat, AND every `(building_id, owner_id)` that appears in the invoices but has no roster row gets an inactive, non-primary flat. Detaching an owner deletes `building_owners` and keeps their invoices on purpose, so any building running a while has such orphans — miss them and `flat_id` stays null forever, `FINALIZE` can never run, and those invoices sit guarded by neither index. **Acceptance test is the file's own closing notice: `invoices still unlinked = 0`.** Pairs with `lib/account-purge.ts`, which now purges `building_owner_flats` by `owner_id`. |
| `FINALIZE_BUILDING_FLAT_INVOICES.sql` | ✅ applied 2026-08-28, **but run BEFORE the deploy — see the postscript** | Whole Building, phase 6b. Sets `building_service_invoices.flat_id` NOT NULL and drops the transitional `building_invoice_legacy_owner_month_idx`, leaving `(flat_id, billing_month)` as the sole guard. Pure hygiene — the feature works completely without it. ⚠️ **Running it early is the one destructive mistake available here**: `NOT NULL` makes every invoice insert from an instance predating the flats deploy fail with `23502`, and the legacy index it drops is what was protecting those inserts from double-billing. It **refuses with a notice** rather than aborting when any `flat_id` is still null (a rolled-back transaction tells you nothing about which row), and does `SET NOT NULL` before the drop so a failure rolls back with the guard intact. ⚠️ **IN PRACTICE THIS WAS RUN BEFORE THE DEPLOY, NOT DAYS AFTER IT.** Both files were applied back to back on 2026-08-28 while production was still serving the pre-flats code. For the window between that and the deploy landing, every building service-charge invoice INSERT failed with `23502` (the old code writes no `flat_id`), i.e. "Issue an invoice" and "Generate the month" were down for building admins. Nothing was lost or mis-billed — a rejected insert writes nothing — and the deploy closed it. The lesson stands for the next migration of this shape: the soak period is not a formality, it is the only thing separating a nullable column from an outage.
